CREATE TABLE `user` (
`id` INTEGER PRIMARY KEY AUTOINCREMENT, 
`Name` TEXT,
);

CREATE TABLE `pins` (
`id` INTEGER PRIMARY KEY AUTOINCREMENT, 
`Comments` TEXT,
user_id INTEGER,
FOREIGN KEY(user_id) REFERENCES user(id)
);